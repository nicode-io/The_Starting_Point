class Product {
    constructor(id, categoryId, subCategory, title, price, color, isNew) {
        this.id = id;
        this.categoryId = categoryId;
        this.subCategory = subCategory;
        this.title = title;
        this.price = price;
        this.color = color;
        this.isNew = isNew;
    }
}

export default Product
